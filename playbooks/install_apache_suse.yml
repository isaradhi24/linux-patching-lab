---
- name: Install Apache Web Server
  hosts: webservers
  become: true

  tasks:
    - name: Install Apache
      apt:
        name: apache2
        state: present
      notify:
        - Start Apache

  handlers:
    - name: Start Apache
      service:
        name: apache2
        state: started
        enabled: yes